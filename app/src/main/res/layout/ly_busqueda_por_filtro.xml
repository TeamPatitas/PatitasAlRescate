<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F4F1E8"
    android:padding="20dp">

    <!-- TÍTULO -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🐾 Búsqueda por filtro"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#5B3A29"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"/>

    <!-- SUBTÍTULO -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Encuentra a tu nuevo compañero"
        android:textSize="14sp"
        android:textColor="#7A6A5E"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"/>

    <!-- CAMPO DE TEXTO -->
    <EditText
        android:id="@+id/txtFiltro"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ej. perro, gato, labrador"
        android:padding="14dp"
        android:background="@android:drawable/edit_text"
        android:layout_marginBottom="16dp"/>

    <!-- BOTÓN -->
    <Button
        android:id="@+id/btnBuscar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="BUSCAR 🐶"
        android:textAllCaps="true"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#7A4A32"
        android:layout_marginBottom="20dp"/>

    <!-- LISTA DE RESULTADOS -->
    <ListView
        android:id="@+id/listResultados"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:dividerHeight="8dp"
        android:padding="4dp"/>

</LinearLayout>
